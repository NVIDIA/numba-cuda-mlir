# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

on:
  workflow_call:
    inputs:
      host-platform:
        required: true
        type: string
      cuda-version:
        required: true
        type: string
      prev-cuda-version:
        required: true
        type: string

defaults:
  run:
    shell: bash --noprofile --norc -xeuo pipefail {0}

permissions:
  contents: read  # This is required for actions/checkout

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        python-version:
          # - "3.10"
          - "3.11"
          # - "3.12"
          # - "3.13"
          # - "3.14"
          # - "3.14t"
    name: py${{ matrix.python-version }}
    runs-on: ${{ (inputs.host-platform == 'linux-64' && 'linux-amd64-cpu8') ||
                 (inputs.host-platform == 'linux-aarch64' && 'linux-arm64-cpu8') ||
                 (inputs.host-platform == 'win-64' && 'windows-2022') }}
    steps:
      - name: Checkout ${{ github.event.repository.name }}
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          fetch-depth: 0

      - name: Set up Python
        id: setup-python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: "3.12"

      - name: Install latest rapidsai/sccache
        if: ${{ startsWith(inputs.host-platform, 'linux') }}
        run: |
          curl -fsSL "https://github.com/rapidsai/sccache/releases/latest/download/sccache-$(uname -m)-unknown-linux-musl.tar.gz" \
            | sudo tar -C /usr/local/bin -xz --strip-components=1 --wildcards '*/sccache'
          sccache --version
          echo "SCCACHE_PATH=/usr/local/bin/sccache" >> "$GITHUB_ENV"
          echo "SCCACHE_GHA_USE_PREPROCESSOR_CACHE_MODE=true" >> "$GITHUB_ENV"

      - name: Install sccache for Windows
        if: ${{ inputs.host-platform == 'win-64' }}
        run: |
          curl -fsSL https://github.com/mozilla/sccache/releases/download/v0.10.0/sccache-v0.10.0-x86_64-pc-windows-msvc.tar.gz -o sccache.tar.gz
          mkdir -p sccache-bin
          tar -xzf sccache.tar.gz -C sccache-bin --strip-components=1
          echo "$(pwd)/sccache-bin" >> "$GITHUB_PATH"
          echo "SCCACHE_PATH=$(pwd)/sccache-bin/sccache.exe" >> "$GITHUB_ENV"
          export PATH="$(pwd)/sccache-bin:$PATH"
          sccache --version

      # xref: https://github.com/orgs/community/discussions/42856#discussioncomment-7678867
      - name: Adding addtional GHA cache-related env vars
        uses: actions/github-script@v9
        with:
          script: |
            core.exportVariable('ACTIONS_CACHE_SERVICE_V2', 'on');
            core.exportVariable('ACTIONS_CACHE_URL', process.env['ACTIONS_CACHE_URL'] || '');
            core.exportVariable('ACTIONS_RESULTS_URL', process.env['ACTIONS_RESULTS_URL'] || '');
            core.exportVariable('ACTIONS_RUNTIME_URL', process.env['ACTIONS_RUNTIME_URL'] || '');
            core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env['ACTIONS_RUNTIME_TOKEN'] || '');

      - name: Setup proxy cache
        if: ${{ startsWith(inputs.host-platform, 'linux') }}
        uses: nv-gha-runners/setup-proxy-cache@main
        continue-on-error: true
        with:
          enable-apt: true

      - name: Set environment variables
        env:
          HOST_PLATFORM: ${{ inputs.host-platform }}
          PY_VER: ${{ matrix.python-version }}
          SHA: ${{ github.sha }}
        run: ./ci/tools/env-vars build

      - name: Dump environment
        run: |
          env

      - name: Determine cibuildwheel and LLVM Python tags
        id: wheel-tags
        run: |
          PY_VER_FMT=$(echo "${{ matrix.python-version }}" | tr -d '.')
          echo "llvm-py-tag=cp${PY_VER_FMT}" >> "$GITHUB_OUTPUT"
          if [[ "${{ inputs.host-platform }}" == win* ]]; then
            echo "cibw-build=cp${PY_VER_FMT}-win_amd64" >> "$GITHUB_OUTPUT"
          else
            echo "cibw-build=cp${PY_VER_FMT}-manylinux*" >> "$GITHUB_OUTPUT"
          fi

      - name: Compute LLVM cache keys
        id: llvm-cache-keys
        run: |
          source ci/llvm-version.env
          if [[ "${{ inputs.host-platform }}" == win* ]]; then
            SCRIPT_HASH=$(sha256sum ci/build-windows.sh | cut -c1-12)
            MODERN_VERSION_SHORT=$(echo "$LLVM_MODERN_COMMIT" | cut -c1-12)
            LLVM7_VERSION_SHORT=$(echo "$LLVM7_TAG" | cut -c1-12)
            echo "llvm-modern-key=llvm-modern-windows-amd64-${{ steps.wheel-tags.outputs.llvm-py-tag }}-${MODERN_VERSION_SHORT}-${SCRIPT_HASH}" >> "$GITHUB_OUTPUT"
            echo "llvm7-key=llvm7-windows-amd64-${LLVM7_VERSION_SHORT}-${SCRIPT_HASH}" >> "$GITHUB_OUTPUT"
          else
            ARCH=$(uname -m)
            MODERN_SCRIPT_HASH=$(sha256sum ci/build-llvm-modern.sh | cut -c1-12)
            MODERN_VERSION_SHORT=$(echo "$LLVM_MODERN_COMMIT" | cut -c1-12)
            echo "llvm-modern-key=llvm-modern-linux-${ARCH}-${{ steps.wheel-tags.outputs.llvm-py-tag }}-${MODERN_VERSION_SHORT}-${MODERN_SCRIPT_HASH}" >> "$GITHUB_OUTPUT"
            LLVM7_SCRIPT_HASH=$(sha256sum ci/build-llvm7.sh | cut -c1-12)
            LLVM7_VERSION_SHORT=$(echo "$LLVM7_TAG" | cut -c1-12)
            echo "llvm7-key=llvm7-linux-${ARCH}-${LLVM7_VERSION_SHORT}-${LLVM7_SCRIPT_HASH}" >> "$GITHUB_OUTPUT"
          fi

      - name: Restore LLVM Modern from cache
        id: llvm-modern-cache
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae  # v5.0.5
        with:
          path: llvm-modern-install/
          key: ${{ steps.llvm-cache-keys.outputs.llvm-modern-key }}

      - name: Download LLVM Modern artifacts
        if: steps.llvm-modern-cache.outputs.cache-hit != 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: llvm-modern-install-${{ steps.wheel-tags.outputs.llvm-py-tag }}-${{ inputs.host-platform }}
          path: llvm-modern-install

      - name: Restore LLVM 7 from cache
        id: llvm7-cache
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae  # v5.0.5
        with:
          path: llvm7-install/
          key: ${{ steps.llvm-cache-keys.outputs.llvm7-key }}

      - name: Download LLVM 7 artifacts
        if: steps.llvm7-cache.outputs.cache-hit != 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: llvm7-install-${{ inputs.host-platform }}
          path: llvm7-install

      - name: Fix LLVM artifact permissions
        if: ${{ startsWith(inputs.host-platform, 'linux') }}
        run: |
          chmod -R +x llvm-modern-install/bin/ llvm7-install/bin/ || true
          chmod -R +x llvm-modern-install/lib/ llvm7-install/lib/ || true

      - name: Set up MSVC
        if: ${{ inputs.host-platform == 'win-64' }}
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756  # v1

      - name: Build numba-cuda-mlir wheel
        uses: pypa/cibuildwheel@8d2b08b68458a16aeb24b64e68a09ab1c8e82084  # v3.4.1
        with:
          package-dir: .
          output-dir: dist
        env:
          CIBW_BUILD: ${{ steps.wheel-tags.outputs.cibw-build }}
          CIBW_MANYLINUX_X86_64_IMAGE: quay.io/pypa/manylinux_2_28_x86_64
          CIBW_MANYLINUX_AARCH64_IMAGE: quay.io/pypa/manylinux_2_28_aarch64
          CIBW_ENVIRONMENT_LINUX: >
            MLIR_DIR=/host/${{ github.workspace }}/llvm-modern-install/lib/cmake/mlir
            LIBLLVM7=/host/${{ github.workspace }}/llvm7-install/lib/libLLVM-7.so
            CC="/host/${{ env.SCCACHE_PATH }} cc"
            CXX="/host/${{ env.SCCACHE_PATH }} c++"
            SCCACHE_GHA_ENABLED=true
            ACTIONS_RUNTIME_TOKEN=${{ env.ACTIONS_RUNTIME_TOKEN }}
            ACTIONS_RUNTIME_URL=${{ env.ACTIONS_RUNTIME_URL }}
            ACTIONS_RESULTS_URL=${{ env.ACTIONS_RESULTS_URL }}
            ACTIONS_CACHE_URL=${{ env.ACTIONS_CACHE_URL }}
            ACTIONS_CACHE_SERVICE_V2=${{ env.ACTIONS_CACHE_SERVICE_V2 }}
            SCCACHE_GHA_USE_PREPROCESSOR_CACHE_MODE=${{ env.SCCACHE_GHA_USE_PREPROCESSOR_CACHE_MODE }}
          CIBW_ENVIRONMENT_WINDOWS: >
            MLIR_DIR="$(cygpath -w ${{ env.LLVM_MODERN_INSTALL_DIR }}/lib/cmake/mlir)"
            LLVM70_ROOT="$(cygpath -w ${{ env.LLVM7_INSTALL_DIR }})"
            CMAKE_C_COMPILER_LAUNCHER="$(cygpath -w ${{ env.SCCACHE_PATH }})"
            CMAKE_CXX_COMPILER_LAUNCHER="$(cygpath -w ${{ env.SCCACHE_PATH }})"
          CIBW_REPAIR_WHEEL_COMMAND_LINUX: >
            auditwheel -v repair {wheel} --wheel-dir {dest_dir}
            --exclude libMLIRPythonCAPI.so
            --exclude libMLIRPythonSupport-numba_cuda_mlir.so
            --exclude libLLVM-7.so &&
            "/host/${{ env.SCCACHE_PATH }}" --show-adv-stats &&
            "/host/${{ env.SCCACHE_PATH }}" --show-stats --stats-format=json > /host/${{ github.workspace }}/sccache_NUMBA_CUDA_MLIR.json

      - name: Report sccache stats (numba-cuda-mlir)
        if: ${{ startsWith(inputs.host-platform, 'linux') }}
        uses: ./.github/actions/sccache-summary
        with:
          json-file: sccache_NUMBA_CUDA_MLIR.json
          label: "numba-cuda-mlir"
          build-step: "Build numba-cuda-mlir wheel (Linux)"

      - name: List the numba-cuda-mlir artifacts directory
        run: |
          ls -lahR dist/

      - name: Upload numba-cuda-mlir build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: ${{ env.NUMBA_CUDA_MLIR_CUDA_ARTIFACT_NAME }}
          path: dist/*.whl
          if-no-files-found: error
